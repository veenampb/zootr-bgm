local SEQ = BGM.getIdToSequenceMap()

local MENU_FANFARE = {
    NAME = 'Menu / Fanfare',
    SEQUENCES = {
        SEQ.TITLE,
        SEQ.MENU,
        SEQ.MASTER_SWORD,
        SEQ.BOSS_DEFEATED,
        SEQ.HORSE_GOAL,
        SEQ.HEART_CONTAINER_GET,
        SEQ.GAME_OVER,
    },
}

return MENU_FANFARE
